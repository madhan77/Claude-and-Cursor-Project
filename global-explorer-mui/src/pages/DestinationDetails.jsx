import { Typography, Container } from '@mui/material'

const DestinationDetails = () => {
  return (
    <Container>
      <Typography variant="h4" sx={{ mt: 4 }}>
        DestinationDetails Page - Under Construction with MUI
      </Typography>
    </Container>
  )
}

export default DestinationDetails
